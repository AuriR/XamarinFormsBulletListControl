<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:BulletListControlTester"
             xmlns:cc="clr-namespace:AurisIdeas.Controls;assembly=AurisIdeas.Controls"
             x:Class="BulletListControlTester.MainPage"
             Title="Bullet List Tester"
             NavigationPage.HasNavigationBar="True">

    <ScrollView>

        <StackLayout>
            
            <!-- Note that character font size can mess with the text alignment. Keep that in mind when using different character sizes. -->
            <Label>Large bullet font test:</Label>
            <cc:BulletListControl  
                Items="{Binding ListItems}" 
                BulletCharacterFontSize="50" 
                ListItemFontSize="18"></cc:BulletListControl>

            <Label>Same-sized bullet font test:</Label>
            <cc:BulletListControl  
                Items="{Binding ListItems}" 
                BulletCharacterFontSize="18" 
                ListItemFontSize="18"></cc:BulletListControl>

        </StackLayout>
    </ScrollView>

</ContentPage>
